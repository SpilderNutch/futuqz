server:
  port: 8082

# DataSource Config
# 不进行SSL连接，并且设置数据库时区为亚洲上海:
spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/qz?useSSL=false&serverTimezone=Asia/Shanghai&allowPublicKeyRetrieval=true
    username: root
    password: OUYANGYU
